# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
*.spec.bak

# Virtual environments
.venv/
venv/
ENV/

# JavaScript dependencies
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo

# PyInstaller
build/
dist/

# Logs
*.log
gesture.log

# User config (keep config.json as default template, ignore local changes)
# config.json

# OS
.DS_Store
Thumbs.db

# WorkBuddy internal files
.workbuddy/

# Internal deliverables (not for public release)
deliverables/
creai_ppt/
frontend/
docs/
_hf.json

# PyInstaller spec backup
*.spec.bak

# Model files (large, should be downloaded separately)
# *.task
# SenseVoice ASR 模型 model.int8.onnx 单文件 239MB，超 GitHub 100MB 单文件硬限。
# 用户按 README 指引手动下载到 models/sense-voice/
models/sense-voice/
# HaGRID YOLO 手部检测器：hand_yolov8n.onnx（12MB）已入仓库，供打包与 CI 冒烟；
# .pt 权重仅重新导出 ONNX 时需要，仍不入库。
models/hand_yolov8n.pt

# Claude Code 工作目录（包含 worktree、隔离副本等内部产物）
.claude/
restore_sessions.ps1

# 变焦超分模型与评估产物（模型按需下载到项目根目录，见 evaluate_sr.py）
ESPCN_x2.pb
FSRCNN_x2.pb
Real-ESRGAN_x2plus.onnx
comparison_result.png
hand_test.jpg

# 板书关键点轨迹录制（simulate_draw.py --replay 用）
draw_trace.jsonl

# 原始视频录制（replay_video.py 回放用，体积大，按需录到本地）
raw_capture/

# 重构备份与开发笔记（不进仓库）
refactor_backup_*/
最早对话.md
AC-trae_问题清单.md
AI_CONTRIBUTIONS.md
PLAN.md
DEMO作品帖.md
t.txt

# IDE / 缓存
.trae/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
